apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "edge-ota-server.fullname" . }}-test-connection"
  labels:
    {{- include "edge-ota-server.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "edge-ota-server.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
