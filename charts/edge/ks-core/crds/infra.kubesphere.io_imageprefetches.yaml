apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.15.0
  name: imageprefetches.infra.kubesphere.io
spec:
  conversion:
    strategy: None
  group: infra.kubesphere.io
  names:
    kind: ImagePrefetch
    listKind: ImagePrefetchList
    plural: imageprefetches
    singular: imageprefetch
  scope: Cluster
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ImagePrefetch is the Schema for the imageprefetches API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ImagePrefetchSpec defines the desired state of ImagePrefetch
            properties:
              imageList:
                items:
                  type: string
                type: array
              nodeList:
                items:
                  type: string
                type: array
              planCron:
                type: string
            required:
            - imageList
            - nodeList
            - planCron
            type: object
          status:
            description: ImagePrefetchStatus defines the observed state of ImagePrefetch
            properties:
              LatestExecution:
                format: date-time
                type: string
              status:
                type: string
            required:
            - status
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}