apiVersion: v1
kind: Service
metadata:
  name: {{ include "router-manager.fullname" . }}
  labels:
    {{- include "router-manager.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "router-manager.selectorLabels" . | nindent 4 }}
