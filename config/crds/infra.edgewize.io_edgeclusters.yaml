---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: (unknown)
  creationTimestamp: null
  name: edgeclusters.infra.edgewize.io
spec:
  group: infra.edgewize.io
  names:
    kind: EdgeCluster
    listKind: EdgeClusterList
    plural: edgeclusters
    singular: edgecluster
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.namespace
      name: NameSpace
      type: string
    - jsonPath: .spec.distro
      name: Distro
      type: string
    - jsonPath: .status.status
      name: Status
      type: string
    - jsonPath: .status.edgewize
      name: EdgeWize
      priority: 1
      type: string
    - jsonPath: .status.cloudcore
      name: CloudCore
      priority: 1
      type: string
    - jsonPath: .status.fluentOperator
      name: FluentOperator
      priority: 1
      type: string
    - jsonPath: .status.edgewizeMonitor
      name: EdgewizeMonitor
      priority: 1
      type: string
    - jsonPath: .status.ksCore
      name: KS-Core
      priority: 1
      type: string
    - jsonPath: .status.kubefed
      name: Kubefed
      priority: 1
      type: string
    - jsonPath: .status.modelmesh
      name: ModelMesh
      priority: 1
      type: string
    - jsonPath: .status.hamiDevicePlugin
      name: HamiDevicePlugin
      priority: 1
      type: string
    - jsonPath: .status.hamiScheduler
      name: HamiScheduler
      priority: 1
      type: string
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: EdgeCluster is the Schema for the edgeclusters API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: EdgeClusterSpec defines the desired state of EdgeCluster
            properties:
              advertiseAddress:
                description: AdvertiseAddress is the advertise address of the Cloudcore
                  in edge cluster, in manual mode
                items:
                  type: string
                type: array
              components:
                description: Components will install in the edgecluster , in auto
                  mode
                items:
                  properties:
                    dependencies:
                      items:
                        type: string
                      type: array
                    file:
                      type: string
                    name:
                      type: string
                    namespace:
                      type: string
                    systemNamespace:
                      type: boolean
                    values:
                      type: string
                  type: object
                type: array
              distro:
                description: 'Distro is Kubernetes distro to use for the virtual cluster.
                  Allowed distros: k3s, k0s, k8s, eks (default "k3s"), in auto mode'
                type: string
              dnsNames:
                description: DNSNames is the dns names of the Cloudcore in edge cluster,
                  in manual mode
                items:
                  type: string
                type: array
              hostCluster:
                description: HostCluster is the cluster in which the edge cluster
                  will be created and hosted (default to "host") it depends on the
                  multi-cluster component.
                type: string
              kubeConfig:
                description: KubeConfig is the edge cluster kubeconfig, encode by
                  base64 , in manual,auto mode
                format: byte
                type: string
              location:
                description: Location is the location of the current cluster. TODO
                type: string
              namespace:
                description: Namespace is the target namespace in the metacluster
                  where the edge cluster will be installed , in auto mode
                type: string
              type:
                description: Type is the edge cluster installation method, auto or
                  manual
                type: string
              version:
                description: Version is the edge cluster distro version. TODO, in
                  auto mode
                type: string
            type: object
          status:
            description: EdgeClusterStatus defines the observed state of EdgeCluster
            properties:
              cloudcore:
                type: string
              componentStatus:
                additionalProperties:
                  type: string
                type: object
              components:
                additionalProperties:
                  properties:
                    dependencies:
                      items:
                        type: string
                      type: array
                    file:
                      type: string
                    name:
                      type: string
                    namespace:
                      type: string
                    systemNamespace:
                      type: boolean
                    values:
                      type: string
                  type: object
                type: object
              configFile:
                type: string
              edgeOtaServer:
                type: string
              edgewize:
                type: string
              edgewizeMonitor:
                type: string
              eventbus:
                type: string
              fluentOperator:
                type: string
              hamiDevicePlugin:
                type: string
              hamiScheduler:
                type: string
              ksCore:
                type: string
              kubeConfig:
                description: KubeConfig is the edge cluster kubeconfig, encode by
                  base64
                type: string
              kubefed:
                type: string
              modelmesh:
                type: string
              routerManager:
                type: string
              status:
                description: Status is the edge cluster release installation status
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
